---
type: doc
status: done
updated: 2026-08-10
---

# Sub-workspace: `apps/web`

The slice of the workspace for work scoped to `apps/web/` alone. Same types, same rules,
same repository as the root at [`../../`](../../README.md). Its `INDEX.md` covers only
this directory; the root `INDEX.md` covers everything and says where each file lives.
